# config is not auto-imported here to avoid module-level side effects (loguru logs,
# env.yaml reads) when rissk.* modules are imported during Kedro runs.
# Import rissk.config explicitly in legacy scripts that need it.
