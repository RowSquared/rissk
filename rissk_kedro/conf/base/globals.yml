# Survey Configuration (from env.yaml)

# survey:
#   name: "hies2024"
#   questionnaires:
#     - name: "snb_hies_hh"
#       VERSION: [9, 10, 11]
#       filter_var: null
#     - name: "slbhies_listing"
#       VERSION: [5, 6, 7]
#       filter_var: null  # Set to a single-key dict to filter by consent, e.g.:
      #   filter_var: {consent_q: "1"}
      # Only interviews where paradata variable 'consent_q' has answer "1" are scored.
      # The answer value must be a string (paradata answers are always strings).
      

# survey:
#   name: "pmpmd"
#   questionnaires:
#     - name: "pmpmd_community"
#       VERSION: [2, 3, 4, 5]
#       filter_var: null
#     - name: "pmpmd_household"
#       VERSION: [4, 5, 6]
#       filter_var: null

survey:
  name: "slchbs"
  questionnaires:
    - name: "slchbs_saintlucia_2025"
      VERSION: [6, 7] # 5 is for testing empty data handling
      filter_var: null 

# survey:
#   name: "fbf house holduntitled folder"
#   questionnaires:
#     - name: "fbf_household"
#       VERSION: [13]
#       filter_var: null